name: Tmate Session

on:
  workflow_dispatch:

jobs:
  tmate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: tmate
        run: sudo apt-get install -y tmate; tmate -F
